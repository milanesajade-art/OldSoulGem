(()=>{const publishKey='vida_published_collection_v1';const defaults=Array.isArray(window.VIDA_COLLECTION_DEFAULTS)?window.VIDA_COLLECTION_DEFAULTS:[];const read=()=>{try{return JSON.parse(localStorage.getItem(publishKey)||'null')}catch{return null}};const pieces=()=>{const p=read()?.pieces;return Array.isArray(p)&&p.length?p:defaults};const id=new URLSearchParams(location.search).get('id');const piece=pieces().find(p=>p.id===id);function set(id,value){const el=document.getElementById(id);if(el&&value!=null)el.textContent=value}function linkFor(name){return `index.html?interest=${encodeURIComponent(name)}#inquire`}function render(){const main=document.getElementById('genericProduct');if(!piece||piece.status==='Hidden'){if(main)main.innerHTML='<div class="product-copy"><p class="eyebrow">VIDA</p><h1>Piece unavailable</h1><p>This piece is not currently part of the public Vida collection.</p><div class="actions"><a class="btn light" href="index.html#collection">Back to Collection</a></div></div>';document.title='Piece unavailable | Vida Collection by Alé';return}main.dataset.pieceId=piece.id;set('genericEyebrow',`${piece.id} • ${String(piece.status||'').toUpperCase()}`);set('genericName',piece.name);set('genericPrice',piece.price||'Private');set('genericStory',piece.story||'');set('genericMaterials',piece.materials||'Fine jewelry');set('genericStatus',piece.status||'Private');set('genericAvailability',piece.price||'Private inquiry');set('genericReference',piece.id);set('genericDesignStory',piece.story||'This piece is part of the evolving Vida collection.');set('genericVisualName',piece.name);const visual=document.getElementById('genericVisual');if(visual&&piece.image){visual.classList.remove('vida-placeholder');visual.innerHTML=`<img src="${piece.image.replace(/"/g,'&quot;')}" alt="${piece.name.replace(/"/g,'&quot;')}">`}const href=linkFor(piece.name);const inquiry=document.getElementById('productInquiry');const inquiryTop=document.getElementById('productInquiryTop');if(inquiry)inquiry.href=href;if(inquiryTop)inquiryTop.href=href;document.title=`${piece.name} | Vida Collection by Alé`;const desc=document.querySelector('meta[name="description"]');if(desc)desc.setAttribute('content',`${piece.name} — ${piece.story||'a private fine jewelry piece from Vida Collection by Alé.'}`)}document.addEventListener('DOMContentLoaded',render);})();