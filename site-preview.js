(()=>{const key='vida_published_site_v1';function payload(){try{return JSON.parse(localStorage.getItem(key)||'null')}catch{return null}}function text(sel,val){const el=document.querySelector(sel);if(el&&val)el.textContent=val;}function apply(){const p=payload();const s=p?.site;if(!s)return;text('.hero-copy .eyebrow',s.eyebrow);text('.hero-copy h1',s.heroTitle);text('.hero-lede',s.heroLede);text('.hero-copy .actions .btn',s.primaryCta);text('.hero-copy .actions .text-link',s.secondaryCta);text('.manifesto-line',s.atelierTitle);text('.manifesto>p:last-child',s.atelierBody);text('.collection-section .section-head h2',s.collectionTitle);text('.collection-section .section-head>p',s.collectionBody);text('.story-title h2',s.storyTitle);text('.story-copy .lead',s.storyLead);const storyParas=document.querySelectorAll('.story-copy>p');if(storyParas[1]&&s.storyBody)storyParas[1].textContent=s.storyBody;text('.inquire-intro h2',s.inquiryTitle);const inquiryParas=document.querySelectorAll('.inquire-intro>p');if(inquiryParas[1]&&s.inquiryBody)inquiryParas[1].textContent=s.inquiryBody;text('.availability span:last-child',s.availability);}document.addEventListener('DOMContentLoaded',apply);window.addEventListener('storage',e=>{if(e.key===key)location.reload();});})();